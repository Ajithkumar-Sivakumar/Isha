apiVersion: v1
kind: Service
metadata:
  name: address-book-service
  namespace: add-book
spec:
  selector:
    app: address-book
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
